type User = {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  countryCode: string;
  hashPassword: string;
  tosAgreement: boolean;
  active: true;
}

export default User;
